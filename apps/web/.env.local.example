# Browser calls same-origin /api; next.config.js rewrites to the Nest API (see API_PROXY_TARGET).
NEXT_PUBLIC_API_URL=/api
# Optional: where Next proxies /api (default http://127.0.0.1:4000)
# API_PROXY_TARGET=http://127.0.0.1:4000
